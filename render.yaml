services:
  - type: web
    name: ty-music
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:aurora
    startCommand: node server-simple.js
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 18.17.1
